import { AddImageRefDirective } from './add-image-ref.directive';

describe('AddImageRefDirective', () => {
  it('should create an instance', () => {
    const directive = new AddImageRefDirective();
    expect(directive).toBeTruthy();
  });
});
